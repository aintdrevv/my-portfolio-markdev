# Project Structure

This workspace is organized into three main folders:

- `frontend/` - your React/Vite portfolio site
- `backend/` - reserved for Xano backend files if you pull them locally
- `docs/` - setup notes, API notes, and project documentation

Run the frontend from `frontend/`.
